FLAG — INPUT REQUIRED OR SUGGESTION
====================================
This file tracks gaps that need YOUR input. Work continues around them;
each item lists what was stubbed/defaulted so nothing is blocked.

[1] STRIPE KEYS (for escrow payments)
    Needed: STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET in .env
    Until then: payment/escrow flow is NOT built (intentionally deferred —
    building it without keys means untestable code). When you add keys, say
    "build payments" and the Stripe Connect escrow flow gets built.

[2] GOOGLE OAUTH CREDENTIALS
    Needed: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET in .env
    Until then: Google sign-in button exists but errors. Credentials login works.

[3] LIVE METAL PRICE FEED
    lib/agents/price-intelligence.ts uses a STATIC spot-price snapshot
    (USD/metric ton, conservative ranges). For production, plug in:
    - LME API (paid), or metals-api.com (free tier), or scrape MSTC.
    Suggestion: metals-api.com free tier = 50 req/month, enough for a
    daily cron refresh. Needs METALS_API_KEY in .env.

[4] EMAIL / WHATSAPP NOTIFICATIONS
    Match alerts + new messages currently surface only in-app.
    Needed for outbound: RESEND_API_KEY (email) and/or Twilio creds (WhatsApp).
    Suggestion: Resend free tier (100 emails/day) is the fastest path.

[5] VERIFICATION DOCUMENT REVIEW
    The verification request flow (built) sets companies to PENDING.
    Approving them is a human/admin task — fraud queue lives at /admin/flagged.
    Decide who reviews and approves verification requests.

[6] RATE LIMITING STORAGE
    README notes Upstash rate limiting as missing. Needs UPSTASH_REDIS_REST_URL
    + UPSTASH_REDIS_REST_TOKEN, or say "use in-memory" for a dev-only limiter.

[7] *** CRITICAL: YOUR DISK IS FAILING — BACK UP NOW ***
    During this session ~300 project files became unreadable (APFS data-block
    corruption): 57 source files, all of public/ + frames/ (480 images),
    postcss.config.mjs, and the local .git history pack. Even THIS FILE,
    written earlier in the session, went unreadable and had to be rewritten.
    Everything was recovered from GitHub (nothing lost — it was all pushed).
    node_modules and .next were rebuilt fresh.
    ACTION REQUIRED:
      1. Back up anything on this Mac that is NOT on GitHub/iCloud, today.
      2. Run Disk Utility → First Aid on Macintosh HD.
      3. If errors persist, this SSD is dying — migrate to a new machine/drive.
    GitHub (https://github.com/Krishmonpara/scrapbridge) is currently the
    only safe copy of this project. Push after every work session.

    [UPDATE 2026-07-05 — disk space] 8.3GB free after clearing the 2.1GB npm
    cache; a 20GB buffer isn't feasible on this machine. Revised guidance:
    - Keep at least 5GB free; below that git/webpack writes start failing.
    - Safe reclaims when space gets tight: ~/.npm (cleared today, regenerates),
      .next/cache if it grows past ~1GB (regenerates), and unused Docker
      layers via `docker system prune` (ASK first — removes stopped containers).
    - First Aid (step 2 above) needs no free space — still do it.

[9] VERCEL DEPLOY (requested 2026-07-06)
    Prep done: `postinstall: prisma generate` added so Vercel builds work.
    To go live, two things only you can provide:
    1. VERCEL LOGIN — run `npx vercel login` once in a terminal (email link),
       then tell me "deploy" and I'll run the production deploy.
    2. HOSTED POSTGRES — the local Docker DB isn't reachable from Vercel.
       Free options: Neon (neon.tech) or Supabase — create a DB, then set
       DATABASE_URL + AUTH_SECRET + NEXTAUTH_URL in Vercel project env vars.
       Without it the site deploys but shows empty listings (graceful).

[8] .ENV WAS RECONSTRUCTED (secrets regenerated)
    The original .env was destroyed by the disk corruption (it is gitignored,
    so there was no GitHub copy). It was rebuilt with:
    - DATABASE_URL from docker-compose.yml (scrapbridge/scrapbridge@localhost:5433)
    - Fresh AUTH_SECRET/NEXTAUTH_SECRET (all existing login sessions invalidated)
    If you had anything else in .env (API keys etc.), re-add it.
    Suggestion: keep a copy of .env in a password manager since it can't
    live on GitHub.

[9] LEFTOVER .git-dead/ DIRECTORY (3.3 MB)
    The corrupted git directory from the disk-recovery was renamed to
    .git-dead/ and kept as a safety net. The new .git works and all commits
    are pushed to GitHub. Once you confirm the repo is healthy, delete it:
    rm -rf ".git-dead"
    (Kept rather than auto-deleted because removing a git directory is
    destructive and it's only 3.3 MB.)

STATUS LOG
----------
2026-06-12: File created. Items 1-6 non-blocking; work continued.
2026-06-12: Disk corruption event — full recovery from GitHub completed.
2026-06-12: .env destroyed by disk + reconstructed (item 8).
